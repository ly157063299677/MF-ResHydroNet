# Demo Data

Run:

```bash
python scripts/make_demo_data.py
```

to generate `demo_pairs.csv`.

The demo dataset follows the paired-sample schema but is synthetic. It should
only be used to verify the code path.

